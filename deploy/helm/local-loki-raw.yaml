# Loki Helm Chart Override - Raw Logs
# For local/development deployment

# Use unique names to avoid conflicts with loki
fullnameOverride: "loki-raw"
nameOverride: "loki-raw"

# Single instance deployment - all components in one pod
deploymentMode: SingleBinary

# Disable other deployment modes
read:
  replicas: 0
write:
  replicas: 0
backend:
  replicas: 0

loki:
  auth_enabled: false
  
  commonConfig:
    replication_factor: 1
    ring:
      kvstore:
        store: inmemory  # Use in-memory instead of memberlist for single instance
  
  storage:
    type: 'filesystem'
  
  schemaConfig:
    configs:
      - from: 2024-01-01
        store: tsdb
        object_store: filesystem
        schema: v13
        index:
          prefix: index_raw_
          period: 24h
  
  # Minimal limits - no retention (infinite)
  limits_config:
    allow_structured_metadata: false  # Disable to avoid schema complexity
    reject_old_samples: false
    unordered_writes: true
    ingestion_rate_mb: 20  # Higher limit for raw logs
    ingestion_burst_size_mb: 40

# Service account for Loki raw logs
serviceAccount:
  create: true
  name: loki-raw
  annotations: {}

# Disable memberlist service (not needed for single instance)
memberlist:
  enabled: false

# Single binary mode - all components in one pod
singleBinary:
  replicas: 1
  
  persistence:
    enabled: true
    size: 20Gi  # Larger for raw logs
    storageClass: ""
  
  resources:
    limits:
      cpu: 2000m
      memory: 4Gi
    requests:
      cpu: 500m
      memory: 1Gi

# Disable all optional components for minimal setup
monitoring:
  lokiCanary:
    enabled: false
  selfMonitoring:
    enabled: false
    grafanaAgent:
      installOperator: false

# Explicitly disable canary service account
lokiCanary:
  enabled: false

# Disable gateway (nginx) - access Loki directly
gateway:
  enabled: false

# Disable chunks cache
chunksCache:
  enabled: false

# Disable results cache  
resultsCache:
  enabled: false

# Disable enterprise features
enterprise:
  enabled: false

# Disable test pods
test:
  enabled: false

# Disable ruler
ruler:
  enabled: false

# Disable index gateway
indexGateway:
  enabled: false

